"""E2.6 -- quality scoring + series clustering for a city DB.

See `now_quality.cli` for the two entry points (`now-quality score`,
`now-quality series`) and each module's docstring for the design
rationale behind its piece of the ticket.
"""

from __future__ import annotations

__all__: list[str] = []
