{
  "name": "@now/auth",
  "version": "0.1.0",
  "description": "Shared staff identity. now_platform.public.users is the single source of truth; each city database keeps a shadow projection carrying role but never a secret. See docs/ADMIN-CONSOLIDATION.md.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "test": "node --test --experimental-strip-types test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/pg": "^8.11.10",
    "typescript": "^5.7.2"
  }
}
