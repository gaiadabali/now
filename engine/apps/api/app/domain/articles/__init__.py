"""Articles domain — reserved for E3 (retrieval) and later.

No business logic yet. This package exists so the module boundary is in
place before it's needed: `GET /v1/{site}/articles/{slug}` and
`GET /v1/{site}/articles/{id}/rails` land here.
"""
