{
  "name": "@now/engine-api-client",
  "version": "0.1.0",
  "private": true,
  "description": "Generated TypeScript types for the NOW! Engine API OpenAPI schema. Regenerated in CI on every change to app/api/**.",
  "scripts": {
    "generate": "openapi-typescript ../openapi/openapi.json -o src/schema.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "openapi-typescript": "^7.4.0",
    "typescript": "^5.6.0"
  }
}
